<UserControl x:Class="RestaurantApp.Views.UserOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="10">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,0,0" />
            <TextBlock Text="My Orders" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="White"/>
        </DockPanel>

        <!-- Orders List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}" Margin="10"
                  Background="#252526" Foreground="White" BorderBrush="#333333" BorderThickness="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Order ID" DisplayMemberBinding="{Binding OrderId}" Width="70"/>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding OrderDate, StringFormat='{}{0:g}'}" Width="150"/>
                    <GridViewColumn Header="Total" DisplayMemberBinding="{Binding TotalAmount, StringFormat='{}{0:C}'}" Width="80"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>
                    <GridViewColumn Header="Est. Delivery" DisplayMemberBinding="{Binding EstimatedDeliveryTime, StringFormat='{}{0:g}'}" Width="150"/>
                    <GridViewColumn Header="Actions" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Cancel" 
                                        Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding Status, Converter={StaticResource StringToOrderStatusConverter}}"
                                        Background="#3A3A3A" Foreground="White" BorderBrush="#555555" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Order Details -->
        <Expander Grid.Row="2" Header="Order Details" Margin="10" IsExpanded="True" Background="#252526" Foreground="White" BorderBrush="#333333" BorderThickness="1">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Message -->
                <TextBlock Grid.Row="0" Text="{Binding Message}" 
                           Foreground="#90EE90" Margin="0,0,0,10"
                           Visibility="{Binding Message, Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- Order Items -->
                <ListView Grid.Row="1" ItemsSource="{Binding SelectedOrder.OrderItems}" MaxHeight="200"
                          Background="#252526" Foreground="White" BorderBrush="#333333" BorderThickness="1">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Product" DisplayMemberBinding="{Binding ProductName}" Width="200"/>
                            <GridViewColumn Header="Unit Price" DisplayMemberBinding="{Binding UnitPrice, StringFormat='{}{0:C}'}" Width="100"/>
                            <GridViewColumn Header="Quantity" DisplayMemberBinding="{Binding Quantity}" Width="70"/>
                            <GridViewColumn Header="Total" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource OrderItemTotalMultiConverter}">
                                                    <Binding Path="Quantity" />
                                                    <Binding Path="UnitPrice" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Expander>
    </Grid>
</UserControl>

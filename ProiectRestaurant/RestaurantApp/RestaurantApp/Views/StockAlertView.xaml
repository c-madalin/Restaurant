<UserControl x:Class="RestaurantApp.Views.StockAlertView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#1E1E1E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header dark -->
        <DockPanel Grid.Row="0" Background="#333333" Margin="10">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" 
                    Margin="0,5,0,5" 
                    Background="#555555" Foreground="White" BorderBrush="#444444" />
            <TextBlock Text="Low Stock Alert" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" 
                       Foreground="White" Margin="5,0,0,0"/>
        </DockPanel>

        <!-- Low Stock Products ListView -->
        <ListView Grid.Row="1" ItemsSource="{Binding LowStockProducts}" Margin="10" 
                  Background="#252525" Foreground="White" BorderBrush="#444444" BorderThickness="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Category" DisplayMemberBinding="{Binding CategoryName}" Width="150"/>
                    <GridViewColumn Header="Product" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Remaining Quantity" DisplayMemberBinding="{Binding TotalQuantity, StringFormat='{}{0}g'}" Width="150"/>
                    <GridViewColumn Header="Portions Left" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PortionSize, Converter={StaticResource PortionsLeftConverter}, ConverterParameter={Binding TotalQuantity}}"
                                           Foreground="White"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Status" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="Low Stock" Foreground="Orange"
                                           Visibility="{Binding TotalQuantity, Converter={StaticResource StockStatusConverter}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

<UserControl x:Class="RestaurantApp.Views.CartButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="100">
    <Button x:Name="CartButton" Click="CartButton_Click" Padding="10,5" Background="#222" Foreground="White" BorderBrush="#555" BorderThickness="1">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Cart" Margin="5,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="🛒" FontSize="14" VerticalAlignment="Center"/>
            <Border Background="#E53935" CornerRadius="10" Margin="5,0,0,0" 
                    Visibility="{Binding ItemCount, Converter={StaticResource IntToVisibilityConverter}}">
                <TextBlock Text="{Binding ItemCount}" Foreground="White" Margin="5,0" FontSize="10"/>
            </Border>
        </StackPanel>
    </Button>
</UserControl>

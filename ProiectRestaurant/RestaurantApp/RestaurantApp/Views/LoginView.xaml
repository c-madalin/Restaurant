<UserControl x:Class="RestaurantApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#0D1117">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Height="320" Width="320">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Login" FontSize="22" FontWeight="Bold" Foreground="#58A6FF" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Grid.Row="1" Text="Email:" Foreground="#C9D1D9"/>
                <TextBox Grid.Row="2" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Background="#21262D" Foreground="White" Margin="0,5,0,15"/>

                <TextBlock Grid.Row="3" Text="Password:" Foreground="#C9D1D9"/>
                <PasswordBox Grid.Row="4" x:Name="PasswordBox" Background="#21262D" Foreground="White" Margin="0,5,0,15" PasswordChanged="PasswordBox_PasswordChanged"/>

                <TextBlock Grid.Row="5" Text="{Binding ErrorMessage}" Foreground="#F85149" Margin="0,0,0,10"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                <StackPanel Grid.Row="6" Orientation="Vertical">
                    <Button Content="Login" Command="{Binding LoginCommand}" Background="#238636" Foreground="White" Margin="0,0,0,10"/>
                    <TextBlock Text="Don't have an account?" HorizontalAlignment="Center" Foreground="#8B949E" Margin="0,10,0,5"/>
                    <Button Content="Register" Command="{Binding RegisterNavigationCommand}" Background="#2196F3" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
<UserControl x:Class="RestaurantApp.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#121212">
    <Grid>
        <Border Width="500" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1" 
                CornerRadius="5" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="500">
                <StackPanel>
                    <TextBlock Text="Register" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="White"/>

                    <TextBlock Text="First Name:" Margin="0,10,0,5" Foreground="White"/>
                    <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Padding="5" Background="#333" Foreground="White" BorderBrush="#555"/>

                    <TextBlock Text="Last Name:" Margin="0,10,0,5" Foreground="White"/>
                    <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Padding="5" Background="#333" Foreground="White" BorderBrush="#555"/>

                    <TextBlock Text="Email:" Margin="0,10,0,5" Foreground="White"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="5" Background="#333" Foreground="White" BorderBrush="#555"/>

                    <TextBlock Text="Password:" Margin="0,10,0,5" Foreground="White"/>
                    <PasswordBox x:Name="PasswordBox" Padding="5" Background="#333" Foreground="White" BorderBrush="#555" PasswordChanged="PasswordBox_PasswordChanged"/>

                    <TextBlock Text="Confirm Password:" Margin="0,10,0,5" Foreground="White"/>
                    <PasswordBox x:Name="ConfirmPasswordBox" Padding="5" Background="#333" Foreground="White" BorderBrush="#555" PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

                    <TextBlock Text="Phone Number:" Margin="0,10,0,5" Foreground="White"/>
                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Padding="5" Background="#333" Foreground="White" BorderBrush="#555"/>

                    <TextBlock Text="Delivery Address:" Margin="0,10,0,5" Foreground="White"/>
                    <TextBox Text="{Binding DeliveryAddress, UpdateSourceTrigger=PropertyChanged}" 
                             TextWrapping="Wrap" AcceptsReturn="True" Height="60" Padding="5" Background="#333" Foreground="White" BorderBrush="#555"/>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF6666" Margin="0,10,0,0" 
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                    <Button Content="Register" Command="{Binding RegisterCommand}" Margin="0,20,0,0" Padding="10,5" Background="#4CAF50" Foreground="White" BorderBrush="#388E3C"/>

                    <TextBlock Text="Already have an account?" HorizontalAlignment="Center" Margin="0,20,0,5" Foreground="White"/>
                    <Button Content="Login" Command="{Binding LoginNavigationCommand}" 
                            HorizontalAlignment="Center" Padding="10,5" Margin="0,0,0,10" Background="#333" Foreground="White" BorderBrush="#555"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>

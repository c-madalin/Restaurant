<UserControl x:Class="RestaurantApp.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#121212">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="10">
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" Padding="10,5"
                    Background="#333" Foreground="White" BorderBrush="#555" BorderThickness="1"/>
            <TextBlock Text="Manage Categories" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="White"/>
        </DockPanel>

        <!-- Categories List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Margin="10"
                  Background="#1E1E1E" Foreground="White" BorderBrush="#333" BorderThickness="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="400"/>
                    <GridViewColumn Header="Actions" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Margin="0,0,5,0" Click="EditButton_Click" 
                                            Background="#2196F3" Foreground="White" BorderBrush="#1769AA" BorderThickness="1" Padding="5,2"/>
                                    <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                                            Background="#B00020" Foreground="White" BorderBrush="#880000" BorderThickness="1" Padding="5,2"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="BorderBrush" Value="#333"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#333"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#555"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Add/Edit Form -->
        <Border Grid.Row="2" BorderBrush="#333" BorderThickness="0,1,0,0" Padding="10" Background="#1E1E1E">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Message -->
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Message}" 
                           Foreground="#4CAF50" Margin="0,0,0,10"
                           Visibility="{Binding Message, Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- Form Title -->
                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding IsEditing, Converter={StaticResource BoolToStringConverter}, ConverterParameter='Edit Category|Add New Category'}" 
                           FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="White"/>

                <!-- Name -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewCategory.Name, UpdateSourceTrigger=PropertyChanged}" Padding="5"
                         Background="#333" Foreground="White" BorderBrush="#555" BorderThickness="1"/>

                <!-- Description -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Description:" VerticalAlignment="Top" Margin="0,10,10,0" Foreground="White"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding NewCategory.Description, UpdateSourceTrigger=PropertyChanged}" 
                         TextWrapping="Wrap" AcceptsReturn="True" Height="60" Padding="5" Margin="0,10,0,0"
                         Background="#333" Foreground="White" BorderBrush="#555" BorderThickness="1"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="10,5" Margin="0,0,10,0"
                            Background="#555" Foreground="White" BorderBrush="#444" BorderThickness="1"/>
                    <Button Content="Add" Command="{Binding AddCommand}" Padding="10,5" Background="#4CAF50" Foreground="White"
                            Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>
                    <Button Content="Update" Command="{Binding UpdateCommand}" Padding="10,5" Background="#2196F3" Foreground="White"
                            Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
